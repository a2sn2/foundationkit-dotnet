# FoundationKit وVisual Studio

افتح `FoundationKit.sln` باستخدام بيئة تدعم .NET 10. الحل النشط يحتوي reusable Core packages وWorkbench وtests وComposer/CatalogGenerator فقط.

لتجربة SQL الكاملة استخدم Workbench، وللتعديلات على الـCore شغل اختبارات الحل وrepository verification قبل فتح PR.
