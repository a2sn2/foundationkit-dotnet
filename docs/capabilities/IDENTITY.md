# Identity Capability

`FoundationKit.Identity` provides account-policy vocabulary, account notification ports, security-event vocabulary, and step-up requirements.

It deliberately does not provide a user store, EF identity schema, OAuth/OIDC server, SMTP transport, or application-specific account copy. Those choices belong to a consuming identity composition/provider.

Current maturity: `ReferenceOnly`.
