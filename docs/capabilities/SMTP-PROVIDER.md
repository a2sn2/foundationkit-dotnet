# SMTP Provider

`FoundationKit.Notifications.Smtp` is a narrow provider adapter over `FoundationKit.Notifications`. It validates transport options and exposes bounded observer diagnostics.

It does not own secrets, relay governance, templates, routing/fallback, retries, bounce processing, or durable delivery history.

Current maturity: `ReferenceOnly`.
