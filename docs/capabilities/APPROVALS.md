# Approvals Capability

`FoundationKit.Approvals` composes a narrow approve/reject decision model with permission-first eligibility, maker-checker enforcement, Workflow resolution, and audit intent.

Sequential/parallel/quorum approval, delegation, escalation, routing, and persistence are outside v1.

Current maturity: `ReferenceOnly`.
