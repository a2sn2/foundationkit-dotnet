# Notifications Capability

`FoundationKit.Notifications` provides bounded channel-neutral `NotificationMessage`, `INotificationSender`, and delivery-result contracts with safe diagnostics.

Templates, user preferences, routing/fallback, durable queues, retry policy, delivery history, and provider credentials remain separate capabilities/provider concerns.

Current maturity: `ReferenceOnly`.
