# Workflow Capability

`FoundationKit.Workflow` is a deterministic state/trigger transition kernel with bounded audit intent. It does not own workflow persistence, scheduling, task routing, BPMN execution, or migration of historical workflow definitions.

Current maturity: `ReferenceOnly`. The package and tests exist; broader adoption/compatibility evidence is intentionally not asserted by the active Core-only repository.
