# Feature Management Capability

`FoundationKit.FeatureManagement` evaluates bounded Boolean feature definitions through Settings with explicit defaults and fail-closed invalid explicit configuration.

Percentage rollout, experiments, targeting, vendor SDKs and organization semantics are not included.

Workbench provides runtime reference evidence. Current maturity: `ReferenceOnly`.
